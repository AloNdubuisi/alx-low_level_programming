#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * _strdup - return length of a string
 * @s: char type
 * Return:  length of string
 */
int _strdup(char *s)
{
	int a;

	for (a = 0; s[a] != '\0'; a++)
	{

	}
	return (a);
}
